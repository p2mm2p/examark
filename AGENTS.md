# AGENTS.md

## 语言

本仓库统一使用**简体中文**写作。所有面向人阅读的产物——文档、`CONTEXT.md`、issue、ticket、提交信息、代码注释、PR 描述——默认使用中文。仅当明确面向非中文读者时才考虑英文。领域术语的规范译名见 `CONTEXT.md`。

## Agent 技能

### Issue 跟踪

本仓库的 issue 使用 GitHub Issues 跟踪。参见 `docs/agents/issue-tracker.md`。

### Triage 标签

五个标准 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。参见 `docs/agents/triage-labels.md`。

### 领域文档

单上下文（single-context）布局：根目录一个 `CONTEXT.md`，ADR 位于 `docs/adr/`。参见 `docs/agents/domain.md`。
